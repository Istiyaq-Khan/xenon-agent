[project]
name = "xenon-agent-skill-linear"
version = "0.1.0"
description = "Linear integration skill for Xenon Agent (Linear's official MCP server)."
requires-python = ">=3.10"
dependencies = ["mcp", "httpx", "xenon-agent-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/linear"]
