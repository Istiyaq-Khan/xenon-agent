[project]
name = "xenon-agent-skill-notion"
version = "0.1.0"
description = "Notion integration skill for Xenon Agent (Notion's official hosted MCP server)."
requires-python = ">=3.10"
dependencies = ["mcp", "httpx", "xenon-agent-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/notion"]
